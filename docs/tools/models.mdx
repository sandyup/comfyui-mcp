---
title: "Models"
description: "Search, download, list, and remove models; manage embeddings and VRAM."
icon: "box"
---

<Info>10 tools. Generated from the live MCP tool schemas — do not edit by hand; run `npm run docs:gen`.</Info>

## search_models

Search HuggingFace Hub for models usable in ComfyUI (checkpoints, LoRAs, VAEs, ControlNets, etc.). Read-only and network-only: queries HuggingFace over HTTP, does NOT require a running ComfyUI or COMFYUI_PATH and does not download anything. Returns a ranked list with modelId, author, downloads, likes, and tags. Pick a result's download URL and pass it to download_model to install it locally. For packs of custom nodes (not models) use search_custom_nodes.

### Parameters

<ParamField path="query" type="string" required>
  Search query (e.g. 'SDXL', 'flux', 'controlnet')
</ParamField>
<ParamField path="filter" type="string">
  Optional HuggingFace pipeline/library tag to narrow results, e.g. 'diffusers' or 'text-to-image'
</ParamField>
<ParamField path="limit" type="integer">
  Max results to return (default 10)
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "search_models",
  "arguments": {
    "query": "<query>"
  }
}
```

---

## download_model

Download a model file to the connected ComfyUI's models directory from a URL (HuggingFace, direct HTTP(S), s3://, or Azure Blob). PREFER this over a raw shell download (curl/wget) for model weights: it lands the file in the right models/ subfolder. LOCAL ComfyUI: streams to disk and surfaces live progress in the panel download tray. REMOTE ComfyUI: dispatches the fetch to the ComfyUI host via the ComfyUI-Manager install-model HTTP API (downloaded server-side; a per-request `auth` header can't be forwarded). target_subfolder accepts any relative subfolder (incl. nested, e.g. 'loras/&lt;subdir&gt;').

### Parameters

<ParamField path="url" type="string" required>
  Direct download URL for the model file
</ParamField>
<ParamField path="target_subfolder" type="string" required>
  Target subfolder under ComfyUI models/. Standard names: checkpoints, loras, vae, upscale_models, controlnet, embeddings, clip, diffusers, diffusion_models, gligen, hypernetworks, photomaker, style_models, text_encoders, unet. Any other relative subfolder (incl. nested like 'loras/&lt;subdir&gt;') is allowed; absolute paths and '..' escapes are rejected.
</ParamField>
<ParamField path="filename" type="string">
  Override filename (auto-detected from URL if omitted)
</ParamField>
<ParamField path="auth" type="object">
  Optional per-request authentication for private/gated model URLs. When provided it overrides built-in HuggingFace/CivitAI token handling.
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "download_model",
  "arguments": {
    "url": "<url>",
    "target_subfolder": "<target_subfolder>"
  }
}
```

---

## download_civitai_model

Download a model from CivitAI into the connected ComfyUI's models/ directory. Resolves a CivitAI model id (latest version) or a model-version id to a download URL via the CivitAI REST API. LOCAL ComfyUI (COMFYUI_PATH set): streams the file to disk under &lt;COMFYUI_PATH&gt;/models/&lt;target_subfolder&gt;/ and returns the saved absolute path. REMOTE ComfyUI: dispatches the download to the ComfyUI host via the ComfyUI-Manager install-model HTTP API (fetched server-side). Provide at least one of model_id or model_version_id. Gated/early-access models require CIVITAI_API_TOKEN locally (sent as a bearer header, never in the URL); remote Manager-side fetches rely on tokens configured on the ComfyUI host.

### Parameters

<ParamField path="target_subfolder" type="string" required>
  Target subfolder under ComfyUI models/. Standard names: checkpoints, loras, vae, upscale_models, controlnet, embeddings, clip, diffusers, diffusion_models, gligen, hypernetworks, photomaker, style_models, text_encoders, unet. Any other relative subfolder (incl. nested like 'loras/&lt;subdir&gt;') is allowed; absolute paths and '..' escapes are rejected.
</ParamField>
<ParamField path="model_version_id" type="integer">
  CivitAI model-version id (from the URL ?modelVersionId=...). If both model_id and model_version_id are given, this selects the specific version of that model.
</ParamField>
<ParamField path="model_id" type="integer">
  CivitAI model id. The latest version is used unless model_version_id is also provided.
</ParamField>
<ParamField path="filename" type="string">
  Override the saved filename (defaults to the CivitAI file name, or the URL basename).
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "download_civitai_model",
  "arguments": {
    "target_subfolder": "<target_subfolder>"
  }
}
```

---

## list_local_models

List model files available to the connected ComfyUI, grouped by type. Read-only. Queries ComfyUI's /models REST endpoint first (works with remote ComfyUI and respects extra_model_paths.yaml — symlinked / mounted dirs the install-path filesystem scan would miss), then falls back to a filesystem scan of COMFYUI_PATH/models/ when the REST endpoint is unavailable. Size and modified time are only available on the filesystem fallback path. Use to see which models are already available before generating or downloading; use search_models to discover new models on HuggingFace, then download_model to fetch them.

### Parameters

<ParamField path="model_type" type="enum">
  Filter by model type (e.g. 'checkpoints', 'loras'). Lists all types if omitted. Options: `checkpoints`, `loras`, `vae`, `upscale_models`, `controlnet`, `embeddings`, `clip`, `diffusers`, `diffusion_models`, `gligen`, `hypernetworks`, `photomaker`, `style_models`, `text_encoders`, `unet`.
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "list_local_models",
  "arguments": {}
}
```

---

## remove_model

Delete a model file from the local ComfyUI models directories. Resolves the path across ALL configured roots — the primary &lt;COMFYUI_PATH&gt;/models AND every directory in extra_model_paths.yaml / extra_models_config.yaml (e.g. models stored on another drive like E:\) — the same roots ComfyUI loads from. The path must stay within a known root (path traversal and absolute escapes are rejected). LOCAL-ONLY: deletes from the local filesystem, so it is not supported against a remote ComfyUI (remove the file on the host).

### Parameters

<ParamField path="path" type="string" required>
  Model file path relative to the ComfyUI models/ directory (e.g. 'checkpoints/sd_xl_base_1.0.safetensors'). The leading segment is the category used to locate the file in extra roots too.
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "remove_model",
  "arguments": {
    "path": "<path>"
  }
}
```

---

## list_extra_paths

View ComfyUI extra search-path config for standalone/manual installs and ComfyUI Desktop. Standalone uses &lt;COMFYUI_PATH&gt;/extra_model_paths.yaml; Desktop uses the OS app-data extra_models_config.yaml. Reports generic categories, so model categories and custom_nodes entries are both visible when present. Read-only.

### Parameters

<ParamField path="target" type="enum">
  Config target: auto chooses Desktop config if it exists, otherwise standalone; standalone uses &lt;COMFYUI_PATH&gt;/extra_model_paths.yaml; desktop uses the OS app-data extra_models_config.yaml. Options: `auto`, `standalone`, `desktop`.
</ParamField>
<ParamField path="config_path" type="string">
  Explicit YAML config path override, mainly for advanced/manual installs.
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "list_extra_paths",
  "arguments": {}
}
```

---

## add_extra_path

Add a directory to a ComfyUI extra search-path YAML config. Use this for model categories such as checkpoints/loras/vae and, on ComfyUI builds that support it, custom_nodes. Writes the config file and returns the updated view; restart ComfyUI to apply.

### Parameters

<ParamField path="target" type="enum">
  Config target: auto chooses Desktop config if it exists, otherwise standalone; standalone uses &lt;COMFYUI_PATH&gt;/extra_model_paths.yaml; desktop uses the OS app-data extra_models_config.yaml. Options: `auto`, `standalone`, `desktop`.
</ParamField>
<ParamField path="config_path" type="string">
  Explicit YAML config path override, mainly for advanced/manual installs.
</ParamField>
<ParamField path="group" type="string">
  Top-level YAML group to edit. Defaults to comfyui_mcp.
</ParamField>
<ParamField path="category" type="string" required>
  ComfyUI search-path category, e.g. checkpoints, loras, vae, diffusion_models, unet_gguf, or custom_nodes.
</ParamField>
<ParamField path="path" type="string" required>
  Directory path to add/remove for that category. Absolute paths are safest; relative paths are resolved by ComfyUI.
</ParamField>
<ParamField path="is_default" type="boolean">
  Set is_default on a newly-created group. Existing groups are not overwritten.
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "add_extra_path",
  "arguments": {
    "category": "<category>",
    "path": "<path>"
  }
}
```

---

## remove_extra_path

Remove a directory from a ComfyUI extra search-path YAML config. Matches the stored path exactly. Restart ComfyUI after removing an active path.

### Parameters

<ParamField path="target" type="enum">
  Config target: auto chooses Desktop config if it exists, otherwise standalone; standalone uses &lt;COMFYUI_PATH&gt;/extra_model_paths.yaml; desktop uses the OS app-data extra_models_config.yaml. Options: `auto`, `standalone`, `desktop`.
</ParamField>
<ParamField path="config_path" type="string">
  Explicit YAML config path override, mainly for advanced/manual installs.
</ParamField>
<ParamField path="group" type="string">
  Top-level YAML group to edit. Defaults to comfyui_mcp.
</ParamField>
<ParamField path="category" type="string" required>
  ComfyUI search-path category, e.g. checkpoints, loras, vae, diffusion_models, unet_gguf, or custom_nodes.
</ParamField>
<ParamField path="path" type="string" required>
  Directory path to add/remove for that category. Absolute paths are safest; relative paths are resolved by ComfyUI.
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "remove_extra_path",
  "arguments": {
    "category": "<category>",
    "path": "<path>"
  }
}
```

---

## get_embeddings

List textual-inversion embeddings installed on the connected ComfyUI server (read from its /api/embeddings endpoint, i.e. the models/embeddings folder). Requires a running, reachable ComfyUI (local or remote); takes no parameters. Returns the embedding names; reference them in positive or negative prompts as embedding:name (e.g. embedding:easynegative). Read-only.

<Note>This tool takes no parameters.</Note>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "get_embeddings",
  "arguments": {}
}
```

---

## clear_vram

Free GPU VRAM by unloading cached models from ComfyUI. Use this between generation runs with different model families (e.g. switching from SDXL to Flux) or when running low on VRAM. Optionally unload only models or only memory.

### Parameters

<ParamField path="unload_models" type="boolean" required default="true">
  Unload all cached models (default: true)
</ParamField>
<ParamField path="free_memory" type="boolean" required default="true">
  Free cached memory/intermediates (default: true)
</ParamField>

### Example

<Note>Example coming soon — the call below is a generated skeleton.</Note>

```json
{
  "tool": "clear_vram",
  "arguments": {
    "unload_models": true,
    "free_memory": true
  }
}
```

---
